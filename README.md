# This is and implementation of an HTTP server ([RFC9112](https://datatracker.ietf.org/doc/html/rfc9112)) in C

## Commands

- `make build` - compiles the server code and produces an executable in the root directory
